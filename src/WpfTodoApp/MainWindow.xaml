<Window x:Class="WpfTodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="clr-namespace:Prism.Regions;assembly=Prism.Wpf"
        xmlns:vm="clr-namespace:WpfTodoApp.ViewModels"
        Title="{Binding WindowTitle}"
        Height="680" Width="1060"
        MinHeight="500" MinWidth="780"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource Brush.Background}"
        FontFamily="{StaticResource Font.Main}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="28" Height="28" CornerRadius="7"
                            Background="{StaticResource Brush.Primary}" Margin="0,0,10,0">
                        <TextBlock Text="T" FontSize="15" FontWeight="Bold"
                                   Foreground="White" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="TaskFlow" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource Brush.TextPrimary}"
                               VerticalAlignment="Center" />
                </StackPanel>

                <!-- Tab Buttons (bound to MainWindowViewModel.MenuItems) -->
                <ListBox Grid.Column="1" ItemsSource="{Binding MenuItems}"
                         SelectedItem="{Binding SelectedMenuItem}"
                         HorizontalAlignment="Right"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="TabBorder"
                                    Background="Transparent" CornerRadius="6"
                                    Padding="14,6" Margin="2,0" Cursor="Hand">
                                <TextBlock Text="{Binding Text}" FontSize="13"
                                           VerticalAlignment="Center" />
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsLogout}" Value="True">
                                    <Setter TargetName="TabBorder" Property="Margin" Value="12,0,2,0" />
                                    <Setter TargetName="TabBorder" Property="TextElement.Foreground"
                                            Value="{StaticResource Brush.TextMuted}" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Prism Main Content Region -->
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ContentRegion"
                        Margin="28,24,28,24" />
    </Grid>
</Window>
