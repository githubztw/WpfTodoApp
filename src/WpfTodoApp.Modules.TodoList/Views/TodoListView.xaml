<UserControl x:Class="WpfTodoApp.Modules.TodoList.Views.TodoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{StaticResource Font.Main}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="我的任务" FontSize="22" FontWeight="Bold"
                           Foreground="{StaticResource Brush.TextPrimary}" />
                <TextBlock Text="管理您的日常待办事项" FontSize="12"
                           Foreground="{StaticResource Brush.TextMuted}" Margin="0,2,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Filter pills -->
                <Border Background="{StaticResource Brush.Surface}"
                        BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                        CornerRadius="8" Padding="3" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="全部" Width="64" Height="30" FontSize="12"
                                Command="{Binding FilterAllCommand}" />
                        <Button Content="未完成" Width="72" Height="30" FontSize="12"
                                Command="{Binding FilterIncompleteCommand}" />
                        <Button Content="已完成" Width="72" Height="30" FontSize="12"
                                Command="{Binding FilterCompletedCommand}" />
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource Button.Primary}"
                        Content="+ 添加任务" FontSize="13"
                        Command="{Binding AddTodoCommand}" Height="38" />
            </StackPanel>
        </Grid>

        <!-- Task List -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Todos}"
                 Background="Transparent" BorderThickness="0"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource Brush.Surface}"
                            BorderBrush="{StaticResource Brush.Border}" BorderThickness="1"
                            CornerRadius="10" Padding="16,14" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Checkbox -->
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}"
                                      VerticalAlignment="Top" Margin="0,5,14,0" />

                            <!-- Content -->
                            <StackPanel Grid.Column="1">
                                <TextBlock FontSize="14" FontWeight="SemiBold" Margin="0,0,0,4">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                    <Setter Property="TextDecorations" Value="Strikethrough" />
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                    <Run Text="{Binding Title}" />
                                </TextBlock>

                                <WrapPanel Margin="0,0,0,0">
                                    <!-- Created time -->
                                    <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MM-dd HH:mm}'}"
                                               FontSize="11" Foreground="{StaticResource Brush.TextMuted}"
                                               VerticalAlignment="Center" />

                                    <!-- Due date -->
                                    <Border CornerRadius="3" Padding="5,1" Margin="8,0,0,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource Brush.PrimaryLight}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="Background" Value="#F3F4F6" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="10">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Primary}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                            <Run Text="{Binding DueDate, StringFormat='截止 {0:MM-dd HH:mm}'}" />
                                        </TextBlock>
                                    </Border>

                                    <!-- Priority -->
                                    <Border CornerRadius="3" Padding="5,1" Margin="6,0,0,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#FEF3C7" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Priority}" Value="0">
                                                        <Setter Property="Background" Value="#F3F4F6" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Priority}" Value="2">
                                                        <Setter Property="Background" Value="#FEE2E2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="10">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#92400E" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Priority}" Value="0">
                                                            <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Priority}" Value="2">
                                                            <Setter Property="Foreground" Value="#991B1B" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                            <Run Text="{Binding Priority, StringFormat='P{0}'}" />
                                        </TextBlock>
                                    </Border>
                                </WrapPanel>
                            </StackPanel>

                            <!-- Actions -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal"
                                        VerticalAlignment="Center" Margin="12,0,0,0">
                                <Button Content="编辑" Width="60" Height="30" FontSize="12"
                                        Style="{StaticResource Button.Secondary}" Margin="0,0,6,0" />
                                <Button Content="删除" Width="60" Height="30" FontSize="12"
                                        Style="{StaticResource Button.Danger}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Empty State -->
        <TextBlock Grid.Row="1" Text="还没有任务，点击「+ 添加任务」开始吧"
                   FontSize="14" Foreground="{StaticResource Brush.TextMuted}"
                   HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Todos.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
