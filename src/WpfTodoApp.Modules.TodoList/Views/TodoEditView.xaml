<UserControl x:Class="WpfTodoApp.Modules.TodoList.Views.TodoEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Style="{StaticResource Card}" Width="480" Padding="36">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.1" />
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="{Binding DialogTitle}"
                           FontSize="{StaticResource FontSize.XL}" FontWeight="Bold"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           Margin="0,0,0,24" />

                <!-- Title -->
                <TextBlock Text="任务标题 *" FontSize="{StaticResource FontSize.SM}"
                           FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBox Style="{StaticResource Input.TextBox}"
                         Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16" />

                <!-- Description -->
                <TextBlock Text="描述（可选）" FontSize="{StaticResource FontSize.SM}"
                           FontWeight="SemiBold" Margin="0,0,0,6" />
                <TextBox Style="{StaticResource Input.TextBox}"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                         Margin="0,0,0,16" />

                <!-- Due Date + Priority Row -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="截止日期" FontSize="{StaticResource FontSize.SM}"
                                   FontWeight="SemiBold" Margin="0,0,0,6" />
                        <DatePicker SelectedDate="{Binding DueDate}" Height="40" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="优先级" FontSize="{StaticResource FontSize.SM}"
                                   FontWeight="SemiBold" Margin="0,0,0,6" />
                        <ComboBox SelectedIndex="{Binding Priority, Mode=TwoWay}" Height="40">
                            <ComboBoxItem Content="低" />
                            <ComboBoxItem Content="中" />
                            <ComboBoxItem Content="高" />
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- Error -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource Brush.Danger}"
                           FontSize="{StaticResource FontSize.SM}" Margin="0,0,0,16" TextWrapping="Wrap" />

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Width="90" Height="40"
                            Style="{StaticResource Button.Secondary}"
                            Command="{Binding CancelCommand}" Margin="0,0,10,0" />
                    <Button Content="保存" Width="90" Height="40"
                            Style="{StaticResource Button.Primary}"
                            Command="{Binding SaveCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
